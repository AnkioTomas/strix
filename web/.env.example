# Copy to web/.env and edit. Shell exports still win over this file.
# cp .env.example .env

# --- Web API ---
STRIX_API_KEY=dev-local
# STRIX_API_AUTH_DISABLED=0
STRIX_API_HOST=127.0.0.1
STRIX_API_PORT=8787
# STRIX_API_DATA_DIR=./data

STRIX_ALLOW_PRIVATE_TARGETS=1
STRIX_MAX_CONCURRENT=1
STRIX_MIN_FREE_MEMORY_GB=0.5
STRIX_MAX_LOAD_PER_CPU=8

# Whole-task wall clock in seconds (also accepts STRIX_MAX_TASK_TIME)
STRIX_TIMEOUT=1800

# --- Strix LLM / infra (loaded into process env) ---
STRIX_LLM=openai/mtplx-flash-next-bare-speed
LLM_API_KEY=sk-local
OPENAI_API_BASE=http://127.0.0.1:8000/v1
LLM_TIMEOUT=300
LLM_STREAM_IDLE_TIMEOUT=300
STRIX_DOCKER_TIMEOUT=300
STRIX_GIT_TIMEOUT=300
